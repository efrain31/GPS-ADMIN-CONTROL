# Framework principal
fastapi==0.115.0

# Servidor ASGI
uvicorn[standard]==0.30.0

# ORM y base de datos
sqlalchemy==2.0.34
databases==0.9.0          # útil para conexiones async
psycopg2-binary==2.9.9    # PostgreSQL (si usas Postgres)
pymysql==1.1.1            # MySQL/MariaDB (opcional, si usas MySQL)

# Autenticación y seguridad
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # hashing de contraseñas
bcrypt==4.2.0

# Validaciones y manejo de datos
pydantic==2.9.2
pydantic-settings==2.4.0           # para manejar configuración con .env

# Utilidades
python-dotenv==1.0.1               # para variables de entorno
email-validator==2.2.0             # validación de correos electrónicos

# Opcionales pero recomendados
alembic==1.13.2                    # migraciones de base de datos
httpx==0.27.2                      # cliente HTTP async (tests/integraciones)
python-multipart==0.0.9
